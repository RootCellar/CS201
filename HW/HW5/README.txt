Compile by using ./build.sh
Note: This assumes that FLTK is installed. I did this homework on Ubuntu.

Each program only has one .cpp file, because I could not figure out how to
compile multiple .cpp files for one program using FLTK.
